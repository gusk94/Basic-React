"use strict";
var count = 0;
count += 1;
// count = "wow" // -> error!
var message = 'hello world';
var done = true;
var numbers = [1, 2, 3];
var messages = ['hello', 'world'];
// messages.push(1); // -> 숫자 넣어지지 않음
var mightBeUndefined = undefined; // string or undefined
var nullableNumber = null; // number or null
var color = 'red'; // red or yellow or orange
color = 'yellow';
// color = 'green' // error
